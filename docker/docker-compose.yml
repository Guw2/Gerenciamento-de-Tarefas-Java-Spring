services:
    mysql:
        image: mysql:8.0
        container_name: mysql-container
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: SS_user_auth
        ports:
            - "3306:3306"
        volumes:
            - data:/var/lib/mysql

volumes:
    data: